# Backend (NestJS + Prisma). Контекст сборки — корень репозитория.
FROM node:22-alpine

WORKDIR /app

# Зависимости
COPY backend/package*.json ./
RUN npm ci

# Prisma и исходники (prisma generate требует DATABASE_URL в env при загрузке конфига)
COPY backend/ ./
ENV DATABASE_URL="postgresql://build:build@localhost:5432/build"
RUN npx prisma generate
RUN npm run build

EXPOSE 3000

# Миграции при старте, затем сервер
CMD ["sh", "-c", "npx prisma migrate deploy && node dist/src/main.js"]
