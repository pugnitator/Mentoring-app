# Frontend Dockerfile для dev-сценария
FROM node:22-alpine

WORKDIR /app

# Копируем package файлы
COPY frontend/package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем исходный код
COPY frontend/ .

# Expose порт для Vite dev server
EXPOSE 5173

# Запускаем dev server с host 0.0.0.0 для доступа извне контейнера
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
